# Supabase config with real credentials (ignore locally, but allow GitHub Actions to commit)
# supabase-config.js

# Node modules (if any)
node_modules/

# Environment files
.env
.env.local

# OS files
.DS_Store
Thumbs.db
